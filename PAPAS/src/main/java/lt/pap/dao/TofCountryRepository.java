package lt.pap.dao;

import lt.pap.model.TofCountry;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TofCountryRepository extends JpaRepository<TofCountry, Long>
{
    
}
